<p id="notice"><%= notice %></p>

<h1>Types de produits</h1>

<%= link_to 'Ajouter un type de produit', new_product_type_path, class: 'btn btn-default' %>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @product_types.each do |product_type| %>
      <tr>
        <td><%= product_type.name %></td>
        <td><%= link_to 'Éditer type de produit', edit_product_type_path(product_type), class: 'btn btn-primary btn-sm' %></td>
        <td><%= link_to 'Supprimer', product_type, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
